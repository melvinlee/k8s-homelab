repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: externaldns-unifi
    namespace: infra
    chart: bitnami/external-dns
    version: 9.0.3
    values:
      - values.yaml